import { Contact } from "@/components/playerFrontend";

export const metadata = {
  title: "Europa-Contact",
};

const page = () => {
  return (
    <>
      <Contact />
    </>
  );
};

export default page;
